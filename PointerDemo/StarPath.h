#import <UIKit/UIKit.h>


// Returns a star bezier path, to use as sample. The star is centered to fit in
// the midddle of the given size, and its dimentions are 30% bigger than the
// smaller side. Just because.
extern UIBezierPath *StarPath(CGSize size);


